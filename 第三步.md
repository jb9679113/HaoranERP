实现 AppLayout 和 Sidebar 组件，严格按以下设计：

侧边栏宽 220px，白色背景 #FFFFFF，无阴影边框用 border-r
左上角：品牌名「山青浩然」大字 + 「羽毛球管理系统」小字

菜单分两个分组，分组标题灰色小字：

【进销管理】
- 综合仪表盘（图标 LayoutDashboard）→ /dashboard
- 商品仓库（图标 Package）→ /products
- 采购入库（图标 ShoppingCart）→ /purchases
- 销售出库（图标 TrendingUp）→ /sales
- 员工管理（图标 Users）→ /employees
- 客户管理（图标 UserCheck）→ /customers

【经营流水】
- 流水列表（图标 List）→ /ledger/transactions
- 快速记账（图标 PlusCircle）→ /ledger/new
- 经营报表（图标 BarChart2）→ /ledger/reports
- 系统设置（图标 Settings）→ /ledger/settings

当前激活菜单项：背景色 #3B6FE8，文字白色，其余灰色
根据 useAuth 的 role 隐藏无权限的菜单项

底部固定：用户头像缩写圆形 + 姓名 + 角色标签（管理员/仓库员/销售员）+ 退出按钮

移动端（< md 断点）：侧边栏隐藏，顶部汉堡按钮触发抽屉