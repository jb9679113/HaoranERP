现在完成以下三个基础模块：

1. lib/supabase.js
   - 用 VITE_SUPABASE_URL 和 VITE_SUPABASE_ANON_KEY 创建 Supabase client 单例

2. hooks/useAuth.js
   - 监听 supabase.auth.onAuthStateChange
   - 登录后从 employees 表查询当前用户的 role（admin/warehouse/sales）
   - 暴露 { user, role, loading, signOut }

3. App.jsx
   - 配置 React Router v6 路由
   - 实现 ProtectedRoute 组件：未登录跳转 /login，角色不符跳转 /403
   - 路由权限：
     /dashboard → 仅 admin
     /products → admin/warehouse/sales
     /purchases → admin/warehouse
     /sales → admin/sales
     /employees → 仅 admin
     /customers → admin/sales
     /ledger/* → 仅 admin